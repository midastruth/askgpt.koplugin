-- AskGPT插件配置文件示例
-- 将此文件重命名为configuration.lua并根据需要修改配置

local CONFIGURATION = {
  -- AI服务基础URL配置
  -- 支持OpenAI API格式或其他兼容服务
  base_url = "http://192.168.0.19:8000",  -- 你的AI服务地址
  
  -- 或者使用Reader AI专用配置
  reader_ai_base_url = "http://192.168.0.19:8000",
  
  -- API密钥（如果需要）
  api_key = "",  -- 可选：如果服务需要认证
  
  -- 文档ID（用于上下文跟踪）
  document_id = "koreader_document",
  
  -- 语言设置
  language = "auto",  -- "auto"表示自动检测，也可以设置为"zh", "en"等
  
  -- 功能特性配置
  features = {
    -- 字典查询的目标语言（包含翻译功能）
    translate_to = "zh",  -- 可选：zh, en, ja, ko等，启用字典按钮
    
    -- 字典查询语言
    dictionary_language = "auto",  -- 字典查询的默认语言
    
    -- 摘要长度控制
    summary_length = "medium",  -- short, medium, long
  },
  
  -- 网络请求配置
  network = {
    timeout = 10,  -- 请求超时时间（秒）
    retry_attempts = 3,  -- 最大重试次数
    retry_delay = 2,  -- 重试间隔（秒）
  },
  
  -- 高级配置
  advanced = {
    -- 自定义端点路径
    reader_ai_dictionary_path = "/ai/dictionary",
    reader_ai_summarize_path = "/ai/summarize",
    reader_ai_analyze_path = "/ai/analyze",
    
    -- 请求头配置
    custom_headers = {
      -- ["Authorization"] = "Bearer your_api_key",
      -- ["Custom-Header"] = "value",
    },
  },
}

return CONFIGURATION
